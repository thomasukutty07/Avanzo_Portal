# API (optional — defaults to http://127.0.0.1:8000)
# VITE_API_BASE_URL=http://127.0.0.1:8000

# When true, the app accepts demo accounts from src/lib/dummyAuth.ts without calling the backend.
# Production builds should leave this unset or false.
VITE_DUMMY_AUTH=true
