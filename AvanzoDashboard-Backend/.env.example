SECRET_KEY=change-me-to-a-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173

# --- Database (optional) ---
# Default if omitted: sqlite file at project root → db.sqlite3
# Use a second file for feature work; remove DATABASE_URL to use db.sqlite3 again.
# Run commands from AvanzoDashboard-Backend/ so relative paths resolve here.
# DATABASE_URL=sqlite:///db.dev.sqlite3
